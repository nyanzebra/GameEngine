#include <window/camera/camera.h>

#include <test/testsuite.h>

using namespace cppe::test;
using namespace cppe::math;
using namespace ftl::graphics;

TEST_SUITE(Camera) {

    TEST("look_at") {
        camera c;

        //glm::mat4 v = c.look_at(glm::vec3(2, 2, 2), glm::vec3(0, 0, 0), glm::vec3(0, 1, 0));
    };

};
